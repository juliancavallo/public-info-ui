import Animation from '../../common/images/loading.gif'

export const LoadingAnimation = ({width}) => {
    return (
            <img src={Animation} alt="Animation gif" className='animation'></img>
    );
}